// lib/core/web_cache_stub.dart

/// Fallback implementation for native platforms (iOS, Android, macOS, Windows).
void killServiceWorkers() {
  // Intentionally empty: Service workers do not exist on native devices.
}
